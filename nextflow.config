
process {
    withName:DOWNLOAD {
	container = 'cuda.sif'
    }
    withName:DATASET {
	container = 'cuda.sif'
    }
    withName:MODEL {
	container = 'cuda.sif'
    }
}

singularity {
    enabled = true
    runOptions = '--nv'
}
